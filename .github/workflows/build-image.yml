name: Build prometheus telegram bot image

env:
  REGISTRY: docker.pkg.github.com
  IMAGE_NAME: prombot
  QUAY_REPO: quay.io/kubernetes-operators
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
on:
  push:
    paths:
      - '**/Dockerfile'

jobs:
  build:
    name: "Build Docker image"
    runs-on: ubuntu-latest
    steps:

      - name: Setup main tag
        run:  |
          if [[ ${GITHUB_REF##*/} -eq "master" ]]; then
          IMAGE_TAG=latest
          else
          IMAGE_TAG=${GITHUB_REF##*/}
          fi
          echo ${IMAGE_TAG}

#
#      - name: Login to github docker registry
#        run: docker login $REGISTRY  -u ${GITHUB_ACTOR} -p ${{ secrets.GITHUB_TOKEN }}
#
#      - name: Build the Docker image
#        run:  |
#             docker build --tag $REGISTRY/$GITHUB_REPOSITORY/$IMAGE_NAME:$IMAGE_TAG \
#                          --tag $REGISTRY/$GITHUB_REPOSITORY/$IMAGE_NAME:$GITHUB_SHA \
#                          --tag $QUAY_REPO/$IMAGE_NAME:$GITHUB_SHA .
#
#      - name: Push image
#        run: docker push $REGISTRY/$GITHUB_REPOSITORY/$IMAGE_NAME:$IMAGE_TAG
#
#      - name: Get quay.io credentials
#        run: echo -n ${{ secrets.QUAY_DOCKER_CFG }} | base64 -d  > ~/.docker/config.json
#
#      - name: Push to quay.io
#        run: docker push $QUAY_REPO/$IMAGE_NAME:$IMAGE_TAG

